/**
 * @description Centralized routing path definitions.
 * Simplifies route changes and references throughout the app.
 */
export const PATHS = {
  DASHBOARD: '/',
  EXPENSES: '/expenses',
  NOT_FOUND: '*'
};

export default PATHS;
